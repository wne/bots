name: NPM

on:
  push:
    branches:
      - master

  schedule:
    - cron:  '*/5 * * * *'

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - run: npx caz --version
      - run: npx caz --version
      - run: npx caz --version
      - run: npx caz --version
      - run: npx caz --version
      - run: npx caz --version
      - run: npx caz --version
      - run: npx caz --version
      - run: npx caz --version
      - run: npx caz --version
      - run: npm install caz
